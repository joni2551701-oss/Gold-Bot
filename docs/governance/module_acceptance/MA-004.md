# MA-004 — Price Stream

**Type**: Module Acceptance Record (DD-053).
**Module**: GoldBot v1.1 Phase 1 — Module 4, Price Stream.
**PR**: #8 (merged to `main`).
**CI**: GREEN. **Trading Safety**: preserved (not wired into the pipeline).
**Accepted by**: Director Final Review.

## Scope

`data_layer/live_data/`: `stream_event.py` (StreamEvent + state/status/capability
models), `provider.py` (PriceProvider abstract interface),
`twelve_data_provider.py` (adapter over TwelveDataClient),
`price_stream.py` (lifecycle state machine + waiting mode + reconnect +
ordering + graceful shutdown), `stream_manager.py` (multi-asset).

## Basis for acceptance

- Amendments DD-046 (lifecycle), DD-047 (waiting mode + crypto exemption),
  DD-048 (provider abstraction), DD-049 (capability discovery), DD-050
  (graceful shutdown), DD-051 (provider isolation), DD-052 (event
  ordering) all satisfied.
- Vendor-agnostic, multi-asset, clock-injected/deterministic; feeds the
  CandleBuilder (single-writer); never writes memory directly.
- 28 tests incl. a PriceStream → CandleBuilder → MarketMemory integration
  test; full suite green, no regression.

## Related
- `docs/architecture/PRICE_STREAM.md`
- `docs/governance/director/DD-053.md`
