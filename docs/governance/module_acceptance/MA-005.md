# MA-005 — Historical Bootstrap

**Type**: Module Acceptance Record (DD-053).
**Module**: GoldBot v1.1 Phase 1 — Module 5, Historical Bootstrap.
**PR**: #11 (merged to `main`).
**CI**: GREEN. **Trading Safety**: preserved (not wired into the pipeline).
**Accepted by**: Director Final Review.

## Scope

`data_layer/historical_data/`: `bootstrap_state.py`, `bootstrap_progress.py`,
`bootstrap_metrics.py`, `historical_provider.py` (+ cache contract),
`twelve_data_historical_provider.py`, `gap_recovery.py`,
`historical_bootstrap.py`. Additive Module 1 extension:
`TimeframeMemory.merge_closed()` (provenance-preserving).

## Basis for acceptance

- Amendments DD-054 (progress), DD-055 (cancellation), DD-056 (resume),
  DD-057 (metrics), DD-058 (strategy) all satisfied.
- Cold start / warm restart / incremental / recovery flows separated;
  vendor-agnostic HistoricalProvider; windowed gap recovery (no
  fabrication); clock-injected/deterministic; API-minimal.
- 26 tests; full suite green, no regression.

## Follow-up (tracked)

DD-063 (Completion Barrier) and DD-066 (Bootstrap Event Hooks) land as a
small Module 5 follow-up; DD-064 (Integrity Check) and DD-065 (Readiness
API) land in Module 6 (per the approved DD-063–066 split).

## Related
- `docs/architecture/HISTORICAL_BOOTSTRAP.md` (design)
- `docs/governance/director/DD-053.md`
