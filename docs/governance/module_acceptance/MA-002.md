# MA-002 — MemoryReader

**Type**: Module Acceptance Record (DD-053).
**Module**: GoldBot v1.1 Phase 1 — Module 2, MemoryReader.
**PR**: #6 (merged to `main`).
**CI**: GREEN. **Trading Safety**: preserved.
**Accepted by**: Director Final Review (originally recorded as DD-041,
superseded as an identifier by this MA record per DD-053).

## Scope

`data/memory/memory_reader.py`: the read-only DD-031 platform contract
over `MarketMemory` — `get_last_candle`/`get_last_closed`/`get_forming`/
`get_series` (copy-on-read), `revision`, `mode`, `health`, discovery.
`subscribe`/`unsubscribe` (module 7) and `snapshot` (module 9) are
declared for contract visibility and raise `NotImplementedError` until
those modules land (backward-compatible fill-in).

## Basis for acceptance

- DD-031 read-only contract complete; registry abstraction correct.
- Copy-on-read safety preserved; forward-compatible subscribe/snapshot
  signatures.
- No Module 1 API change; isolated from the pipeline.
- 10 unit tests; full suite green, no regression.

## Related
- `docs/architecture/MARKET_DATA_FOUNDATION.md`
- `docs/governance/director/DD-053.md`
