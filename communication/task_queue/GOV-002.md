# GOV-002

**Order**: ORDER-016
**Title**: Core_Worker.md — Core Worker Role Definition
**Track**: Governance v1.1 (per `docs/GOVERNANCE_V1_1_MASTER_PLAN.md`)
**Priority**: Critical
**Status**: ✅ APPROVED — Director review complete (ORDER-016 verdict:
APPROVED; all 15 sections present, Core module split referenced not
restated, Decision Boundaries clear, Trading Safety correctly located
inside the Core Worker's authority, Risk Manager bypass prohibited, AI
layer given no execution/Telegram authority, feature/core conditional
per ORDER-009, no implementation detail). Awaiting Governance v1.1
whole-set Freeze.

## Objective

Produce the authoritative document defining the Core Worker role —
Trading Engine & AI execution — its authority, responsibilities, and
boundaries. Scoped to the Core Worker only (Director, Platform Worker
covered by cross-reference, not definition).

## Deliverable

`docs/governance/roles/Core_Worker.md` — all 15 Director-specified
sections (Purpose, Role Definition, Responsibilities, Authority,
Decision Boundaries, Communication Rules, Reporting Rules, Deliverable
Standards, Quality Requirements, Review Process, Constraints, Out of
Scope, Compliance Requirements, Success Criteria, References).

## TASK 0 — Foundation Reuse Audit (Article 11)

- **`docs/HANDOFF.md` and `docs/CURRENT_PHASE.md`** already record the
  Core/Platform module split at the practical level (Core = `context/`,
  `strategies/`, `signals/`, `decision/`, `risk/`, `ai/`,
  `core/pipeline.py`, "frozen for the Platform role"). Resolution:
  Core_Worker.md is Role-tier (authority/obligations/boundaries) and
  references that split rather than restating or replacing it (§2,
  §12, §13).
- **`CLAUDE.md` Trading Safety** read and layered in as an absolute
  Decision Boundary (§5) — the Core Worker is the role that touches
  `strategies/`/`signals/`/`risk_layer/risk_engine/risk_manager.py`/`decision_layer/decision_engine/decision_engine.py`/
  `execution/`, so the "no modification without explicit per-change
  Director approval" rule is stated exactly where the role that would
  breach it is defined.
- **`docs/governance/roles/Director.md`** (GOV-001, APPROVED) exists —
  Core_Worker.md references the Director's review/approval authority
  (§10) rather than redefining it.
- No new code module created; `docs/governance/roles/` already exists
  from GOV-001.

## Mandatory rules covered (from the brief)

All five present: works from Director ORDERs (§3, §6); makes no
independent architecture decision (§5); writes code/refactoring/
implementation (§2, §3, §4); reports at each task end (§7); task
complete only on APPROVED (§7, §10).

## Constraints covered (from the brief)

All four present (§11): no independent governance-document change; no
repository-strategy change; no branch-policy change; no assumption of
Director authority.

## Acceptance / Exit Criteria (self-check)

- Consistent with Constitution (Articles 1/2/3/6/7/8/11 cross-referenced;
  supremacy stated) — §13.
- Consistent with `Director.md` and `DIRECTOR_POLICY.md`; no
  contradiction, relationships stated — §10, §13.
- No duplicate content (Director/Platform Worker/HANDOFF split all
  referenced, not restated) — §12, §13.
- No implementation detail; role-only — §12.
- Ready for review.

## Constraints (respected by this task)

No new governance document beyond this deliverable and its ticket; no
existing Constitution/Policy/ADR/Standard modified; no repository
implementation; no branch operation; no GitHub settings change.

## Depends on

GOV-001 (Director.md, APPROVED). Per the Master Plan, GOV-002 is a
dependency of GOV-004 (Collaboration_Rules.md), which describes how the
roles interact.

## Notes

Per the Master Plan, GOV-003 (Platform_Worker.md) is the next document
once GOV-002 is APPROVED. Governance v1.1 is declared Frozen only once
all nine GOV documents are individually approved and frozen.
