"""01_Data_Layer / Historical_Data/HistoricalDataFlow — goldbot.data_layer.historical_data.historical_data_flow.

Foundation Freeze v1.0 — canonical architecture skeleton.

Phase A creates this package as an empty, importable placeholder that
mirrors New_Map/. No business logic lives here yet; code is migrated
module by module in Phases B-E under Migration Isolation Rule
(MIR-001). Until a module is migrated, its behaviour still comes from
the pre-freeze top-level packages, which keep working during migration
per Import Compatibility Rule (ICR-001).

Canonical documentation: New_Map/01_Data_Layer/Historical_Data/HistoricalDataFlow/README.md
"""
